// Enums are named following the same convention as classes: WithCapitals

// The variants of enums are written in SCREAMING_SNAKE_CASE

enum CarSeat {
    DRIVER,
    SHOTGUN,
    BACK_LEFT,
    BACK_RIGHT,
}
